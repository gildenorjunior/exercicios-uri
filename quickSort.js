var input = require("fs").readFileSync("stdin", "utf8");
var lines = input.split('\n').shift();

function quickSort(array){
    if(array.length < 2){
        return array
    } else {
        pivo = array[0];
        menores = []
    }
}